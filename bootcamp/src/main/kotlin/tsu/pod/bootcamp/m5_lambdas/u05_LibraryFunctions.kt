package tsu.pod.bootcamp.m5_lambdas

fun main() {

    println("Library Functions")

    /**
     * Functions
     *
     * - filter().size
     *   - count()
     *
     * - sortedBy().reversed()
     *   - sortedByDescending()
     *
     * - map().filterNotNull()
     *   - mapNotNull()
     *
     * - get and initialize (not found)
     *   - getOrPut(lambda)
     *
     * - groupBy()
     *   - fos lists and sequences
     *
     * - groupingBy()
     *   - grouping in a lazy manner
     *   - for sequences
     * - eachCount
     *   - counts elements in each group
     *
     */

}